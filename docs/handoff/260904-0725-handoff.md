# Handoff — Phase 2-5 (2026-09-04 07:25 KST)

## 1. 한 줄 요약

**BL-3 해소로 막혀 있던 것이 전부 풀렸다.** T-2·T-3·T-7이 실행·검증까지 끝났고(T-3·T-7은 조건부 PASS, 잔여는 시한이 정해진 자동 실증뿐), 남은 주경로는 **T-4 자격증명 회전 → Prove 편입**이다. backend-dev가 T-4 조사에 착수했다. **Observer 회답 OB2-D는 발신 완료**이고 회신 대기는 D-1~D-4 4건이다.

## 2. 현재 Phase 상태

- **phase_id**: 2-5 (운영 안정화 + 백업 무결성)
- **current_state**: `IN_PROGRESS`
- **실행 순서 주의**: 번호는 2-5이나 **2-1 직후 / 2-2 이전** (master-plan §4·§5)
- **게이트**: G0 SKIP · G1 PASS · **G2_infra PARTIAL** · G3_smoke PENDING · G4 PENDING

### Task 상태

| Task | status | 잔여 |
|---|---|---|
| T-1 헬스 모니터링 | ✅ `completed` | — |
| T-5 `workspace.json` | ✅ `completed` | — |
| **T-2 자동 커밋·푸시** | ✅ **기능 실증 완료** | 09-03 10:17 첫 발화 → 커밋·푸시 관측. 이후 no-op 정상 |
| **T-3 CouchDB 물리 백업** | ◐ **조건부 PASS** | **세대 회전 실증 — 09-07(월) 04:17**. 현재 5/7세대 |
| **T-7 `/wiki` MOC** | ◐ **조건부 PASS** | G2_infra E2E(`/wiki` 신규 노트→MOC 자동갱신) 미수행 |
| T-4 자격증명 회전 | ◐ **조사 착수** | 계획 보고 → 승인 → 실행 |
| T-6 iPhone LiveSync | ⬜ `pending` | `depends_on: 2-5-4` |

### 차단 요인

- ✅ **BL-3 해소** (2026-09-02, 사용자 터미널 실행). crontab 3줄, `PAB-GIT-AUTOCOMMIT` `17 */2` 등재·발화 확인
- **BL-1** `UK_PAB_VAULT_PUSH_URL` 미발급 — Observer 소관. OB2-D **D-3**으로 재요청 중 (진행 차단 아님)
- **BL-2** 기존 UK 모니터 알림 미도달 원인 미규명 — Observer 소관. OB2-D **D-4**로 재요청 중

## 3. 직전 3 Phase 기억 (CHAIN-13)

- **phase-2-1** (DONE 2026-06-16): CouchDB LiveSync를 3800X에 구축. **bridge 단방향 패치(`Hub.patched.ts`)와 `pabbridge` 읽기전용 계정이 이때 만들어졌고**, 그것이 지금 Prove 편입의 차단 요인이자 설계 근거다
- **phase-1-5**: `/pab:wiki` SSOT skill 어댑터 → ver6-2 이식으로 **`/wiki`로 변경**
- **phase-1-4**: CLI 자동화 `scripts/wiki/wiki.py` — **Prove가 이것을 vendor해 쓰며, 그쪽 수정분이 역으로 우리에게 필요해졌다**(T-7이 그 이식이었다)

## 4. 작업 컨텍스트

- **git**: `main` · 워킹트리 **clean** · 미푸시 **3건**(cron이 짝수시 :17에 자동 푸시)
- **최근 commit**:
  - `0b6dc2e` docs(phase-2-5): **broken 206 정체 규명** — 실행 경로 차이 21, 과거 판정 재검토 등재
  - `6c74b4e` chore(wiki): TOPIC MOC **22건 catch-up 갱신 — 16건 변경** (T-7 이식 후 첫 실행)
  - `cdc78ba` docs(phase-2-5): 반려 건 원인 규명 + 진짜 불일치 1건 발견 (TYPES/SOURCE.md)
  - `c90666a` docs(phase-2-5): T-7 기록 정정 2건 — 해시 갱신 + §5 인식 상태
  - `bf40027` feat(wiki): T-7 기존 TOPIC MOC 갱신 루프 이식 + 판정 모집단 화이트리스트
- **인프라**: 맥북 cron **3줄** / 서버 cron **10줄**(`PAB-COUCHDB-VOL*` 2줄 포함)
- **팀**: backend-dev **active** (T-4 조사 중)

### 이번 세션에서 확정된 것

| # | 내용 |
|---|---|
| T-3 등록 | Observer OB2-C §8.4 정식 수용 → 사용자 승인 → `--volbackup-only` 실행. **프라이밍(백업+리허설) 선행이 핵심**이었다 — 등록 시점 `LAST_OK_TS` 경과가 196.7h(임계 36h)라 없었으면 즉시 FAIL → 15분 뒤 Telegram 오탐 |
| T-3 무인 실증 | **09-04 04:17:02 자동 발화** — 등록 후 사람 개입 없이 돈 첫 사례. 세대 5/7 |
| T-7 이식 | 지침 **13항목**(6종 + N-1~N-7) 전건 이행. 시험 17 + **돌연변이 7종 주입 검증** |
| T-7 catch-up | **22건 대상 / 16건 변경 / 6건 무변경**. 무변경은 `if new_text != text:`가 막은 것 — 그만큼 LiveSync 4홉 전파도 없었다 |
| `git add -A` | **지금은 그대로 두기로 결정.** 근본 해결(스테이징 범위 축소)은 T-2 후속으로 미룸 |
| Observer OB2-D | **발신 완료.** R-1~R-4 전건 + BL-3 해소 통지(R-2) |

### 🔴 반드시 지킬 것

- **본 저장소는 PUBLIC** — 문서·스크립트·로그·커밋에 실제 토큰·chat_id·비밀번호 금지
- **R-1 단일 writer** / **R-4 SOURCE 불변**(`15_Sources/*_source.md`)
- **HR-1** — Team Lead는 `scripts/` 직접 수정 금지. `.claude/skills/`·`docs/`는 가드 밖
- ⚠️ **`pab_sync_healthcheck.sh` 499줄** — HR-5 warn(500) 1줄 전. 확장 시 `check_*` 함수만 분리
- ⚠️ **자동커밋이 짝수시 :17에 `git add -A` → commit → push** 한다. **긴 작업은 그 전에 의미 있는 커밋으로 끊을 것** — 이번 세션에 T-7 작업 중 코드가 `chore(auto)`로 묻힌 사고가 났다

## 5. 다음 작업 프롬프트

```
Phase 2-5를 이어서 진행한다.

[0] 컨텍스트 복구 (HR-3 / FRESH-1)
1. /ssot-reload  — SSOT는 docs/SSOT/, v8.2-renewal-6th (ver6-2)
2. PROJECT.md §3 절대 제약 · §4 게이트 예외 E-1~E-4
3. docs/phases/phase-2-5/phase-2-5-status.md (ENTRY-1)

[1] backend-dev T-4 보고 수령 → 검수
   T-4 = pabadmin·pabbridge 회전 + pabprove 신규 발급 + 쓰기 통과 실증
   ⚠️ probe 설계 제약: `_` 접두는 doc id에도 적용. 밑줄로 시작 안 하는 doc id +
      밑줄 접두 필드 없음, 둘 다 만족해야 VDU에 도달한다.
      **400은 "차단됨"이 아니라 "시험 실패"다.**
      우리는 방향이 반대라 더 조심 — **200이 왔다고 VDU가 통과를 허용한 것은 아니다**
      (VDU에 닿기 전 성공했을 수 있다). 유효 문서 + VDU 발동 경로 확인을 함께 걸 것
   ⚠️ 실제 회전은 계획 검수 후 별도 승인. pabbridge가 미러 체인(홉2)을 물고 있어
      끊기면 Observer #26·#31이 운다. Observer 사전 통지 필요 여부도 판단할 것

[2] T-4 완료 후 → Prove 편입 실행
   Observer 24h 리드타임 재통지 선행 → 편입 → PO8(Prove 통지)
   편입 시 bridge#2를 pab-vault-net에 붙인다(OB2-D R-1 확약).
   ⚠️ 배포 시 docker inspect로 실이름·실IP 측정해 Observer에 확정 통지(R-4).
      그 통지가 #31 화이트리스트 등재 신호다 — 그 전에 넣으면 상시 (absent) 오탐

[3] 시한이 정해진 자동 실증 2건 — 그날 확인만 하면 된다
   09-06(일) 04:42  T-3 복원 리허설 첫 자동 실행
   09-07(월) 04:17  T-3 첫 세대 회전 (7→7 유지, 가장 오래된 것 삭제)
                    → 확인되면 T-3이 조건부 → 확정 PASS

[4] Observer 통지 — 09-10경 묶어서
   T-3 등록 완료 + T-7 완료 + R-2 관찰 1주 데이터를 OB2-E로 한 번에.
   ⚠️ 미래 번호를 예약해 부르지 않는다 — 발신 시점에 채번

[5] 미해결·판단 대기
   · TYPES/SOURCE.md 얼어붙은 MOC — 결함인가 의도적 제외인가
   · broken 206 — 직전 세션의 "존재하지 않는 값" 판정 재검토
   · git add -A 스테이징 범위 (T-2 후속)
   · G3_smoke 장애 주입 — tester 스폰 + 사용자 승인(서비스 중단 수반)
   · docs/history/ 추적 여부 (사용자 결정 대기, PUBLIC 공개가 비가역)
```

---

## 6. 인계 시 놓치기 쉬운 것

### 6.1 외부 채널 상태

| 채널 | 최근 | 우리 대기 | 상대 대기 |
|---|---|---|---|
| **PAB-Observer** | **OB2-D 발신(09-02)** | — | **D-1~D-4 회신** |
| **PAB-Prove** | PO7 왕복 종결 | **PO8**(편입 실행 통지) | **P-1 완료 통지**(미확약) |

**OB2-D 회신 요청 4건**:
- **D-1** `UK_COUCHDB_VERIFY_PUSH_URL` 발급 판정 + **모니터 실번호** (우리 코드에 가번호 `#33`이 박혀 있다)
- **D-2** bridge#2 `#31` **선등재 금지** 확인
- **D-3** BL-1 `UK_PAB_VAULT_PUSH_URL` 발급 상태
- **D-4** BL-2 알림 미도달 원인 규명 상태

⚠️ **OB3는 승격 실행 통지 전용**으로 예약돼 있다. Observer 회답은 **OB2-E**로 채번.
⚠️ Observer 자발 통지: **승격 준비 코드(`PROMOTION_GRACE_UNTIL`) 미배포**. OB3 일정 잡을 때 감안.

### 6.2 ⭐ `broken 206` — 직전 세션 판정을 뒤집을 수 있는 발견

직전 핸드오프가 *"내가 낸 오류 중 최악"* 으로 기록한 건이다: *"존재하지 않는 값(206)을 계약 문서(PO2)에 적었고, 그 차이에 'MOC 정비로 21건 해소'라는 그럴듯한 설명까지 붙였다."*

**이번에 backend-dev가 규명했다 — `206`은 실재하는 값이다.**

| 경로 | `broken` |
|---|---:|
| `obsidian unresolved` (`validate.py:238`) | **185** |
| 정규식 폴백 (`validate.py:240`) | **206** |
| **차이** | **21** ← 과거 불일치와 같은 값 |

`obsidian unresolved`가 어제는 `rc=0`·281줄, 오늘은 **`rc=1`·0줄**(3/3 재현)이라 폴백으로 넘어간다. **vault가 아니라 실행 경로가 값을 바꾼다.** `git worktree`로 구 vault를 재현해 갱신 전후 모두 206임을 확인했으므로 22건 catch-up의 기여는 0이다.

⚠️ **다만 이것도 추론이 남아 있다.** *"206은 재현 가능한 값이다"* 까지가 확정이고, *"PO2에 적힌 206이 폴백 경로에서 나온 것이다"* 는 **미확정**이다 — 그때의 obsidian 동작 여부를 모른다. **틀린 것은 설명이었지 숫자가 아니었을 가능성이 크다**는 선까지만 기록했다.

⇒ `counts.broken`은 FC-14가 읽는 **계약 필드**다. 값이 외부 바이너리 동작에 따라 21 흔들린다는 사실 자체를 Observer·Prove에 알릴지 판단이 필요하다.

### 6.3 이번 세션의 실패 형태 — 전부 같은 뿌리다

| 누가 | 무엇 | 실체 |
|---|---|---|
| Team Lead | `timeout 10 crontab -l \| grep -c` → **0** | macOS에 `timeout` 없음 → `rc=127`, 빈 입력을 셈 |
| Team Lead | `type: *\[\[SOURCE\]\]` grep → **0건** | 실제는 `type: "[[SOURCE]]"` — **따옴표로 패턴이 빗나감** |
| Team Lead | 워처 비교용 전체 해시를 **지어냄** | 앞 12자만 로그에서 본 값, 뒤는 창작 → 즉시 오탐 |
| Team Lead | T-3을 `PASS`로 기록 | 검증 3항목 중 세대 회전 미실증 → **조건부 PASS**로 정정 |
| Team Lead | §5 "원인 확정" | 보고를 그대로 받아 적음 → 재현/추론 분리로 정정 |
| backend-dev | *"TYPES에 2종 없다"* | **`\| tail -35` 절단**. 전체 36줄, 앞 2줄이 정확히 그 둘 |
| backend-dev | *"vault가 146 → 153으로 늘었다"* | **146은 `_attachments`·`40_Templates` 제외값, 153은 전체값.** 같은 기준으로 재면 둘 다 146 |

⚠️ **backend-dev 건이 가장 교훈적이다.** TYPES 건에서 같은 실행의 `총 34 MOC 인식`과 화면상 합계 32의 모순을 **이미 알아채고 속으로 정정 계산까지 했는데, 최초 결론이 정정되지 않은 채 보고에 나갔다.** 측정 실패가 아니라 **관측한 모순을 끝까지 해소하지 않은 것**이다.

> ⭐ **그리고 같은 형태가 하루 만에 재발했다** — 146 vs 153. backend-dev 자기 보고다. `git worktree` 재현으로 결론이 바뀌기 전에 스스로 잡았다.
>
> **두 건의 공통 뼈대: 다른 기준으로 잰 두 수를 나란히 놓고 그 차이를 사실로 읽는다.** TYPES는 *절단된 목록* 대 *전체 계수*, 146/153은 *필터 적용값* 대 *전체값*이었다. 차이가 눈에 보이면 그것을 설명할 기제를 찾게 되고(**`_process_moc`의 `exists()` 건너뛰기 / "vault가 늘었다"**), 기제가 코드상 사실이면 검증이 거기서 멈춘다.
>
> ⇒ **`broken 206`도 정확히 이 형태였다**(§6.2). 185 대 206은 *obsidian 경로* 대 *폴백 경로*였고, 당시 그 차이에 *"MOC 정비로 21건 해소"* 라는 기제가 붙었다. **세 건이 같은 뼈대다.**

**규율 3건이 나왔다**:
1. **부재 주장은 `tail`/`head`를 거친 출력으로 하지 않는다.** `ls` 또는 계수 명령으로만
2. **패턴이 0을 반환한 것과 대상이 0인 것은 다르다.** 명령이 실제로 실행됐는지부터 본다
3. ⭐ **두 수의 차이를 보면 기제를 찾기 전에 "같은 기준으로 잰 값인가"부터 확인한다.** 필터·범위·경로·시점 중 하나라도 다르면 그 차이는 아직 사실이 아니다

> ⭐ **다만 이번엔 방향이 바뀐 것이 있다.** §5 인식 상태(재현 vs 추론)는 **backend-dev가 스스로 갈라냈고**, `broken 206`은 **추정하지 않고 `git worktree`로 재현해서** 확정했다. 직전 세션이 *"혼자 찾을 수 있었던 것은 없다"* 고 적었던 것과 비교하면, 이번엔 자기 보고를 스스로 정정한 사례가 둘 나왔다.

### 6.4 팀 운영 — 이번에 실제로 어긴 것

- **게이트는 작업 지시와 같은 메시지 안에.** 대체로 지켰으나, **T-7 착수 지시에 "7분 뒤 자동커밋이 워킹트리를 쓸어간다"를 넣지 않아** 작업 중 코드가 `chore(auto)`로 묻혔다. backend-dev가 그 위험을 **미리 경고했는데도** 반영하지 못했다
- **답이 이미 닫힌 질문을 하지 않는다.** 10:10에 *"푸시할까요?"* 를 물었는데 7분 뒤 cron이 푸시했다. 물을 게 아니라 의도적으로 푸시하거나 cron을 멈췄어야 한다
- **보고한 커밋을 amend하면 즉시 통지한다.** backend-dev가 이력 연결 줄을 넣으려 amend해 `361327d → bf40027`이 됐고, 그 사이 Team Lead가 기록에 박은 해시 4곳이 죽었다
- **낡은 통지는 알게 된 즉시 정정한다.** 10:18 관측(`?? tests/`)이 10:22에 무효가 된 것을 알고도 앞선 통지를 바로잡지 않았다

### 6.5 결정 대기 (사용자)

- **`TYPES/SOURCE.md`** — 마커를 가진 MOC가 *"(현재 등록된 노트 없음)"* 을 표시하는데 실제 `type: "[[SOURCE]]"` 노트는 41건. `MOC_TYPE_NAMES`(6종)에 `SOURCE`가 없어 루프가 도달하지 않는다. **N-5가 경고한 상수 목록 형태**다. 결함인가 의도적 제외인가
- **`docs/history/` 추적 여부** — `.gitignore`에 "결정 대기" 주석과 함께 보류 중. PUBLIC 공개가 비가역
- **G3_smoke 장애 주입** — 가동 중 서비스를 일부러 중단. tester 스폰 시점 승인 필요
