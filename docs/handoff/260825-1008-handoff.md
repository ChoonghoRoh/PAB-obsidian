# Handoff — Phase 2-5 (2026-08-25)

## 1. 한 줄 요약

CouchDB 3일 장애 복구 → Phase 2-5(운영 안정화) 편성·T-1 완료 → ver6-2 SSOT 이식까지 마쳤다. **다음 세션은 PAB-Prove 협업 관계를 협의 문서로 먼저 확인한 뒤 진행한다** — Prove 산출물 승격이 임박했는데 PAB-obsidian↔PAB-Prove 직접 채널이 아직 없다.

## 2. 현재 Phase 상태

- **phase_id**: 2-5 (운영 안정화 + 백업 무결성)
- **current_state**: `IN_PROGRESS`
- **실행 순서 주의**: 번호는 2-5이나 **2-1 직후 / 2-2 이전**에 실행한다 (master-plan §4·§5 명시). AutoCycle이 번호순으로 동작하면 별도 통제 필요
- **게이트**: G0 SKIP · G1 PASS · **G2_infra PARTIAL**(T-1 한정) · G3_smoke PENDING · G4 PENDING

**Task 진행**

| Task | 상태 |
|---|---|
| T-1 헬스 모니터링 | ✅ 완료 — G2_infra 조건부 PASS (Team Lead 독립 검증) |
| T-2 GitHub 백업 | ◐ **부분** — 34일 공백은 커밋으로 해소. **자동 커밋 cron 미구현** |
| T-3 CouchDB 볼륨 백업 | ⬜ 미착수 (일 1회 × 7세대 확정) |
| T-4 자격증명 회전 | ⬜ 미착수 — `pabadmin`·`pabbridge`. **T-1·T-2 완료 후** (PR-1) |
| T-5 `workspace.json` 추적 해제 | ⬜ 미착수 |
| T-6 iPhone LiveSync | ⬜ 미착수 |

**차단 요인**

- **BL-1**: `UK_PAB_VAULT_PUSH_URL` 미발급 (Observer 회신 대기). 미설정 시 Push 생략 동작이라 진행 차단은 아님
- **BL-2**: 기존 UK CouchDB 모니터 알림 미도달 원인 미규명. UK는 15초 주기로 `/_up`을 감시 중이었는데도 3일 장애가 사람에게 닿지 않았다 — 규명 없이는 신규 모니터도 같은 이유로 침묵한다
- **G3_smoke 미수행**: tester 미스폰. 장애 주입은 **가동 중 서비스를 일부러 중단**시키므로 사용자 승인 필요

## 3. 직전 3 Phase 기억 (CHAIN-13)

- **phase-2-1** (완료 2026-06-16): 다기기 동기화 + 백업 인프라 — CouchDB LiveSync를 3800X에 구축. 잔여 인계(자격증명 회전·iPhone·백업 자동화)가 Phase 2-5로 넘어왔다
- **phase-1-5**: `/pab:wiki` SSOT skill 어댑터 (REWORK 후 PASS+). ver6-2 이식으로 스킬명이 **`/wiki`로 변경**됨
- **phase-1-4**: CLI 자동화 `scripts/wiki/wiki.py`

## 4. 작업 컨텍스트

- **git branch**: `main` · **작업트리 청결** (0 M / 0 ??)
- **최근 commit**:
  - `845bc6a` feat(ssot): ver6-2 이식 — LIFECYCLE-5·6 도입 + SSOT 경로 docs/SSOT 단일화
  - `66bf6d7` docs(phase-2-5): 운영 안정화 Phase 편성 + OB2 회신·승격 사전 통지
  - `03fb4e2` wiki: 미백업 노트 8종 등록 — 오프사이트 백업 34일 공백 해소
  - `21e900f` docs(interop): PAB-Observer 관측 계약 개설 + OB1 수신 *(← Observer 측 발신)*
  - `ce4533f` wiki: Kimi K3 자료 추가

### ⚠️ 환경이 바뀌었다 — 이전 세션 기억과 다름

| 항목 | 변경 |
|---|---|
| SSOT 경로 | 루트 `SSOT/` → **`docs/SSOT/`** (구 트리 제거) |
| SSOT 버전 | v8.3 → **v8.2-renewal-6th** (ver6-2 라인). v8.3 `policy/model-assignment.md`는 이식 보존 |
| 설정 | **`PROJECT.md`** 신설 = 단일 소스 → `/project-config sync` → `hooks.env` |
| `CLAUDE.md` | ver6-2 표준. **HR-1~HR-8 전문 제거**(SSOT가 정본). HR-8 → **NOTIFY-1** 재편 |
| 훅 | 4→**9종**, hook 이벤트 4→**8종** (`SubagentStart/Stop`·`Stop`·`PostCompact` 신규) |
| 스킬 | 10→**18종**. `pab:wiki` → **`/wiki`**. `/menu`로 카탈로그 |
| 신규 | **LIFECYCLE-5·6** — 에이전트 좀비 감지 + 체크 스케줄러(`scripts/zombiecheck/`) |
| `.gitignore` | `SSOT/`·`scripts/` 무시 **해제** (121파일 신규 추적) |

### 합의된 결정

- **DP-2-5-1** 모니터링 = 3800X crontab + 맥북 주 1회 역방향
- **DP-2-5-2** git 자동 커밋 = 맥북 로컬 cron (DP-1 git-authority 무변경)
- **DP-2-5-3** CouchDB 백업 = 일 1회 × 7세대
- **O-2** PAB-Prove 승격 = **임박(이번 Phase 내)** ← 다음 세션 핵심
- **O-4** 서버 증분 60건 = **일부만 선별 회수** (기준 미확정)
- SSOT 경로 = `docs/SSOT/` 이전 · 적용 전 전체 커밋으로 롤백 지점 확보

### 🔴 반드시 지킬 것

- **본 저장소는 PUBLIC**이다. 문서·스크립트에 실제 토큰·chat_id·비밀번호 금지. 비밀값은 `.env`(무시됨)에만
- **R-1 단일 writer**: 미러·CouchDB 직접 쓰기 금지
- **R-4 SOURCE 불변**: `15_Sources/*_source.md` 재페치·덮어쓰기 금지
- 자격증명 `pabadmin`·`pabbridge`가 세션 대화에 노출된 이력 있음 → T-4 회전 필요

## 5. 다음 작업 프롬프트

```
Phase 2-5(운영 안정화 + 백업 무결성)를 이어서 진행한다.
단, 착수 전에 PAB-Prove 협업 관계를 먼저 정리한다.

[0] 컨텍스트 복구 (HR-3 / FRESH-1)
1. /ssot-reload  — SSOT는 이제 docs/SSOT/ 이고 v8.2-renewal-6th(ver6-2 라인)다
2. PROJECT.md 읽기 — 프로젝트 설정 단일 소스 (§1 개요·§3 절대 제약·§4 게이트 예외)
3. docs/phases/phase-2-5/phase-2-5-status.md 읽기 (ENTRY-1)

[1] PAB-Prove 협업 관계 확인 — 이번 세션의 최우선
읽을 문서 (순서대로):
  - docs/interop/README.md                     허브 규약 (SSOT 배지·수신본/회신본)
  - docs/interop/pab-observer/README.md         협업 인덱스 + 회신 대기표
  - .../260824-OB1-vault이원화-크로스체크-점검요청.md   (SSOT=PAB-Observer, 읽기전용)
  - .../260825-OB2-vault이원화-회신-및-승격사전통지.md  (SSOT=PAB-obsidian, 우리가 발신)
  - ~/WORKS/PAB-Khala/docs/interop/pab-prove/README.md  (Khala↔Prove, 참고)

파악해야 할 핵심:
  · vault가 두 벌이다 — PAB-LLMDATA(정보, 화이트리스트 102) /
    PAB-LLMDATA-prove(개발용, 로컬 140 → 서버 pab-wiki-vault 200)
  · PAB-Prove 워커가 서버 개발 vault에 계속 쓰는데 운영 전파는 0이다
    (WIKI_ENV=dev 격리). 서버 증분 약 60건은 로컬로 회수되지 않는다
  · Khala 문서는 Prove가 "PAB-LLMDATA에 생성 → LiveSync 전파"한다고 기술하나
    실태와 다르다. authority가 Prove라 정정은 그쪽 소관 (OB1 §1.4)
  · 우리는 O-2를 "승격 임박(이번 Phase 내)"으로 회신했고, O-3 사전 통지를
    발효했다. 승격 일시가 확정되면 OB3로 재통지해야 한다

판단할 것:
  ① PAB-obsidian ↔ PAB-Prove 직접 interop 채널을 열 것인가
     (현재는 Observer를 통해 간접 인지만 하고 있다. vault 정의 SSOT는
      우리인데 정작 그 vault에 쓰는 주체와 직접 채널이 없다)
  ② 승격 실행 주체·절차·시점 — 누가 무엇을 어디로 옮기는가
  ③ O-4 서버 증분 60건의 선별 기준

[2] Phase 2-5 잔여 Task
  T-2 자동 커밋 cron 구현 (34일 공백은 해소됐으나 자동화 미완)
  T-3 CouchDB 볼륨 백업 (일 1회 × 7세대)
  T-4 자격증명 회전 — T-1·T-2 완료 후 (PR-1)
  T-5 workspace.json git 추적 해제
  T-6 iPhone LiveSync 검증
  * 코드 작업은 HR-1에 따라 backend-dev 스폰 후 위임한다
  * G3_smoke(장애 주입)는 tester 소관이며 서비스 중단을 수반하므로 사용자 승인 필요

[3] Observer 회신 대기 (B-1~B-5) — 도착 시 반영
  UK_PAB_VAULT_PUSH_URL 발급 / 알림 미도달 원인 규명 /
  개발 vault 경량 유실 감시 / 중복 판정
```
