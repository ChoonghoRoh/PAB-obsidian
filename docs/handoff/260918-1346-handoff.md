# Handoff — phase-2-5 (260918-1346)

## 1. 한 줄 요약

**PAB-v4 회원 데이터 룰 조율 2일차** — 규정 정본 **9판본** 수신·검증(R-1~R-13), 우리 소유로 **보관 디렉터리 규약 신설**, 검증 **6축** 정립. **사용자 결정 4건 대기 중이며 그중 하나는 파괴적 변경이다.**

## 2. 현재 Phase 상태

- **phase_id**: 2-5 (운영 안정화 + 백업 무결성)
- **current_state**: **BUILDING**
- **진행 중 작업**: PAB-v4 interop 조율(`PV1`~`PV13` 발신 완료). Phase Task 자체는 T-4·T-6·T-7 미착수
- **차단 요인**:
  - `BL-1` (OPEN) — `UK_PAB_VAULT_PUSH_URL` 미발급, Observer 회신 대기. 진행 차단은 아님(Push 생략 동작)
  - `BL-2` (OPEN) — 기존 UK CouchDB 모니터 알림 미도달 원인 미규명
  - `BL-3` — T-2 cron 활성화, **사용자 터미널 1회 실행 필요**(`deploy_monitoring.sh --local-only`)
  - **T-4** — `PR-1`(T-2 선행) + Prove `R-5` 게이트. ⚠️ **PAB-v4 의존은 없다**(V1 §4로 회원 DB 편입 철회됨)
- **게이트**: G0 SKIP · G1 PASS · G2_infra PARTIAL · G3_smoke PENDING · G4 PENDING
- **팀**: `roles` 전원 `not_spawned` — **코드 수정 전 FRESH-7로 팀 재구성 필수**

## 3. 직전 3 Phase 기억 (CHAIN-13)

- **phase-2-1**: 다기기 동기화 + 백업 인프라 — CouchDB LiveSync + livesync-bridge 4홉 체인 구축
- **phase-1-5**: `/pab:wiki` SSOT skill 어댑터 (DONE)
- **phase-1-4**: CLI 자동화 (`scripts/wiki/wiki.py`) — `moc-build`·`validate` 계열

## 4. 작업 컨텍스트

- **git branch**: `main`
- **git status**: **0 M / 0 ??** — ⚠️ **T-2 자동 커밋(2h 주기)이 어제 작업물 전부를 이미 커밋했다.** 최근 5건이 모두 `chore(auto)`다
- **최근 commit**:
  - `45300d4` chore(auto): vault 자동 백업 — 2개 파일 변경
  - `df1455e` chore(auto): vault 자동 백업 — 5개 파일 변경
  - `bcd1ea7` chore(auto): vault 자동 백업 — 7개 파일 변경
  - `96d4e3b` chore(auto): vault 자동 백업 — 8개 파일 변경
  - `7bd67c2` chore(auto): vault 자동 백업 — 3개 파일 변경
- ⚠️ **채널 규약과 자동화가 충돌한다** — 규약은 *"수신본은 커밋하지 않는다"* 인데 자동 커밋이 v4 발신 수신본까지 잡았다. 미해결

### 합의된 결정

1. 🔴 **회원 관련 데이터 룰은 PAB-v4 소유**(2026-09-16 사용자 결정) — 우리는 자체 룰을 만들지 않고 **문의**하며, 회신 전까지 **동작을 멈춘다**(fail-closed). 정본 수신본: `docs/interop/260916-회원데이터-룰-소유규정-v4정본.md` (**수정 금지**, `interop/` 루트 보관)
2. 🔴 **MOC 분류 체계 소유는 우리**(v4 규정 R-11) — 신규 분류 생성은 **`TOPICS` 계층만**. `TYPES`(7)·`DOMAINS`(6)는 고정 어휘(frontmatter Critical 값이라 늘리면 규격 변경)
3. ✅ **보관 디렉터리 규약 신설**(2026-09-17) — `PAB-LLMDATA/30_Constraints/naming-convention.md` §「밑줄 접두」: **경로의 어느 세그먼트든 `_` 로 시작하면 수집·색인·MOC 등재 제외.** 시드 판본과 **동일 내용**. **파일 이동 없음**(링크 보존)
4. ✅ **R-10 경계 판정 동의** — **vault 표시명은 우리 편집 규칙 밖**이다. ⚠️ **조건부: 「DB 컬럼에만 있는 한」**(vault 안 파일에 기록되면 wiki 문서가 되어 적용)
5. 🔴 **마커 문자열은 양측 공통 계약**(R-12) — `<!-- moc-build:auto-start/end -->`. 한쪽만 바꾸면 **실패가 에러가 아니라 소유권 이동으로 나타난다**
6. 🔴 **Observer 통지 주체 정정** — WebDAV 등 **v4 자산만 바뀌는 변경은 v4 소관**. v4는 Observer와 직접 채널(`OV1` §1 「사전 통지 3종」)을 갖고 있다. ⚠️ **우리 `PV1` §9 근거가 틀렸다**(승격 프로토콜의 24h를 구조 변경에 옮겨 적었음) — 정정 기록은 채널 README §6
7. ✅ **우리 vault 자산이 회원 응답에 실리고 있었다** — v4가 자기 데이터로 확정. **R-6(대리 조회) 확정으로 닫히는 경로**이고 **우리 기술 조치는 0**(미러 공급은 `PROJECT.md` §1 목적 ⑶ 자체). v4가 두 시점 통지 약속(R-6 완료 / 회원 증가)
8. 🔑 **검증 6축을 채널 규약에 등재** — **대상 · 집계 방법 · 시각 · 변하는 값인가 · 화자 · 소스가 여럿일 때 일치의 근거.** 여섯 다 실패로 배웠고 **하나도 Phase 게이트가 잡을 수 없다**

### 🔴 사용자 결정 대기 4건

| # | 항목 | 성격 | 우리 판단 |
|:-:|---|---|---|
| ① | **`moc-build` 에 `_` 제외 반영** | 🔴 **파괴적** — MOC 3곳(`DOMAINS/KNOWLEDGE_MGMT`·`TYPES/RESEARCH_NOTE`·`TOPICS/LLM_WIKI`)에서 항목이 빠지고 **복제로 전 기기 전파**. `scripts/` 라 **HR-1 위임 필요** | 확인 없이 진행하지 않음 |
| ② | **TYPE 어휘 정본 판정** | `PROJECT.md` "6" · `frontmatter-spec:49` "7(SOURCE 없고 INDEX 있음)" · 실물 `TYPES/` 7(SOURCE 있고 INDEX 없음) — **셋 다 다름** | 설계 판단이라 고르지 않음. 실물을 사실로 보는 중 |
| ③ | **`scripts/migrations/` 추적 해제** | v4 **삭제 승인 완료**(정본은 alembic 18개, 참조 코드 없음). 8파일 · `docs/SSOT/GUIDE.md` 참조 6곳 · **LOCK-1으로 Phase 중 SSOT 수정 불가** | **Phase 밖에서 GUIDE 정리와 묶어 처리** |
| ④ | **미러 공급 유지** | 우리 자산이 회원 응답에 계속 실림(R-6 전까지) | **조치 없음 유지 권고** |

### v4 쪽 대기

- `_` 세그먼트 전수 검사 **필터 반영** — 반영되면 prune이 `admin-super`·`member_b` DB 복제본까지 정리(파일 불변). **반영 시 통지 약속**
- `PV2` §7 ③ — `OV1` §1 「사전 통지 3종」 v4 채널 근거 확인 (미결)
- 배지 오인 — R-6으로 안 닫힘, 별건

## 5. 다음 작업 프롬프트

```
Phase 2-5 를 이어갑니다. PAB-v4 interop 조율은 상대 회신 대기 상태이니,
사용자 결정 4건 중 진행 가능한 것부터 처리해 주세요.

우선순위 제안:
1. TYPE 어휘 정본 판정 (결정 ②) — 세 곳 불일치를 실측으로 다시 확인하고
   정본 후보를 근거와 함께 제시해 주세요. 판정은 제가 합니다.
2. 결정 ①(moc-build 반영)은 파괴적 변경이라 제가 승인하면 backend-dev 를
   스폰해 위임하세요. MOC 3곳에서 항목이 빠지는 것을 먼저 보여 주세요.
3. 결정 ③(scripts/migrations 추적 해제)은 LOCK-1 때문에 Phase 밖 작업이니
   지금은 손대지 말고 대기 목록에 두세요.

작업 착수 전 /ssot-reload 를 실행하고, 코드 수정이 필요하면 FRESH-7 에 따라
팀을 재구성하세요. 상대 세션(PAB-v4-0914)에서 `_` 필터 반영 통지가 오면
그때 알려 주세요.
```
