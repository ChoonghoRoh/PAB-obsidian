# Handoff — Phase 2-5 (2026-09-10 08:13 KST)

## 1. 한 줄 요약

**T-3이 확정 PASS로 닫혔고 Prove 채널도 종결됐다.** 남은 주경로는 여전히 **T-4 자격증명 회전**이며, 그 유일한 차단 해제 경로인 **`OB2-E`를 09-08에 발신했으나 Observer 세션이 이틀째 열리지 않아 미도달**이다. 그사이 **MOC 카테고리 역할 분리**가 새 작업으로 들어와 Prove와 합의 중이고, **`TYPES/SOURCE.md` 삭제 판정이 확정돼 사용자 승인만 남았다.**

## 2. 현재 Phase 상태

- **phase_id**: 2-5 (운영 안정화 + 백업 무결성)
- **current_state**: `IN_PROGRESS` ⚠️ SSOT 3-workflow §1.1의 20개 상태 코드 밖 — 정규화 필요
- **실행 순서 주의**: 번호는 2-5이나 **2-1 직후 / 2-2 이전**
- **게이트**: G0 SKIP · G1 PASS · **G2_infra PARTIAL** · G3_smoke PENDING · G4 PENDING

### Task 상태

| Task | status | 잔여 |
|---|---|---|
| T-1 헬스 모니터링 | ✅ 조건부 PASS | — |
| T-2 자동 커밋·푸시 | ✅ 기능 실증 완료 | — |
| **T-3 CouchDB 물리 백업** | ✅ **확정 PASS (09-07)** | **없음.** 회전 09-07·09-08 **2일 연속** + 리허설 09-06 무인 PASS |
| T-4 자격증명 회전 | ◐ **1단계만 완료** (`11f44b0`) | **2~4단계 = Observer `E-1`·`E-2` 회신 대기** |
| T-5 `workspace.json` | ✅ completed | — |
| T-6 iPhone LiveSync | ⬜ pending | `depends_on: 2-5-4` |
| T-7 `/wiki` MOC | ◐ 조건부 PASS | G2_infra E2E 미수행 · 22건 일괄 갱신 미실행 |

### 차단 요인

- 🔴 **Observer 세션 부재** — `OB2-E`(09-08 발신)가 파일로만 대기. **피어 목록에 Observer가 없다**(09-07·09-08·09-10 3회 확인). 도달은 그쪽 세션이 열려 저장소를 읽을 때
- **BL-1** `UK_PAB_VAULT_PUSH_URL` 미발급 — `D-3`/`E-4`로 재요청 중
- **BL-2** UK 모니터 알림 미도달 원인 미규명 — `D-4`/`E-4`
- ✅ **v4 대기 해소** — v4가 *"T-4를 기다리지 않겠다"* 확약. 착수 안 함(자기 문제 6건)

## 3. 직전 3 Phase 기억 (CHAIN-13)

- **phase-2-1** (DONE 2026-06-16): CouchDB LiveSync 구축. **bridge 단방향 패치와 `pabbridge` 읽기전용 계정이 이때 만들어졌고**, 그것이 지금 Prove 편입의 차단 요인이자 설계 근거다
- **phase-1-5**: `/pab:wiki` SSOT skill 어댑터 → ver6-2 이식으로 **`/wiki`로 변경**
- **phase-1-4**: CLI 자동화 `scripts/wiki/wiki.py` — **Prove가 vendor해 쓰며, 그쪽 수정분 역이식이 T-7이었다**

## 4. 작업 컨텍스트

- **git**: `main` · 워킹트리 **clean** · 미푸시 **0**
- **이번 세션 커밋 9건** (09-07 ~ 09-08):
  - `06626d4` **OB2-E 발신** — probe ① 대행 + T-4 회전 작업창 + T-3 확정 PASS
  - `38cc060` `ab25328` `535c717` `d98c693` Observer DRAFT 4회 갱신
  - `133cdb8` **T-3 조건부 → 확정 PASS**
  - `8519b8d` 채널 규약 정본 + PO11 종결
  - `11ecd40` `2a9648b` **PO10·PO9 발신**
- **팀**: **없음**(`team-lead` 단독). status.md `backend_dev: active`는 **낡은 표기 — 정정 필요**

### 이번 세션에서 확정된 것

| # | 내용 |
|---|---|
| **T-3 확정** | 회전 09-07·09-08 **2일 연속**(`20260826-044630`·`044750` 삭제, 7/7 유지) + 리허설 09-06 04:42 무인 PASS. 종전 유보(*"root:root 실파일은 조건이 다르다 — 논증이지 실측이 아니다"*) 해소 |
| ⭐ **R-8 규명** | `counts.broken` 흔들림 원인 = **Obsidian 데스크톱 앱 기동 여부**. 앱 켜짐 185 / 꺼짐 정규식 폴백 206. `status`는 불변이라 **FC-14 계약 영향 없음** |
| **PO6 재정정** | *"206은 존재하지 않는 값"* 이라던 자기 정정을 **재정정**. 206은 실재하며 재현 가능하다. **틀린 것은 숫자가 아니라 그 차이에 붙인 설명**이었다 |
| **Prove 채널 종결** | `R-8`·`R-11`~`R-15`·`PR-8-2` 전건. **양측 미회신 0건** |
| **채널 규약 정본** | 전달(대칭 배치+1줄 고지)·수령(명시적 표명)·채번(발신 시점, **동시 충돌 시 커밋 시각 이른 쪽**)·내용(**근거를 함께 적는다**). `docs/interop/README.md`, **Prove 채널 한정** |
| **P-8 수령** | 자체 감사 스크립트 금지. 개정판에서 **런타임 계측만 조건부 허용**(①교차확인 ②수치≠판정). 세션 메모리 저장 |
| **OB2-E 채번** | 문자 교대가 패턴이었으나 **E를 우리가 썼다** — 교대 규칙이 명문화 안 됨 + 발신 시점 채번 + **E를 비워두는 것 자체가 미래 번호 예약** |
| ⚠️ **OB2-D 미배치 발견** | 09-02 발신본이 Observer 저장소에 없었다. `D-1`~`D-4` 무응답 원인일 수 있다(단정 안 함). `OB2-E`와 함께 배치 |
| 🔑 **SOURCE 판정** | **`00_MOC/TYPES/SOURCE.md`는 제거 대상** — 근거 4건(§5 참조). **삭제는 사용자 승인 대기** |

### 🔴 반드시 지킬 것

- **본 저장소는 PUBLIC** — 토큰·chat_id·비밀번호 금지
- **P-8**: 판정은 코드를 읽어 내린다. 자체 감사기 금지. 런타임 계측만 예외(교차확인·수치≠판정)
- **HR-1** — Team Lead는 `scripts/` 직접 수정 금지. `.claude/skills/`·`docs/`는 가드 밖
- **R-1 단일 writer** / **R-4 SOURCE 불변**
- ⚠️ **짝수시 :17 자동커밋**이 `git add -A`. 긴 작업은 그 전에 끊을 것 — 이번 세션에 **PO8(Prove 수신 계약 문서)이 `chore(auto)`에 묻혀 3일간 미인지**였다
- ⚠️ `pab_sync_healthcheck.sh` **499줄** — HR-5 warn(500) 1줄 전

## 5. 다음 작업 프롬프트

```
Phase 2-5를 이어서 진행한다.

[0] 컨텍스트 복구 (HR-3 / FRESH-1)
1. /ssot-reload — SSOT는 docs/SSOT/, v8.2-renewal-6th (ver6-2)
2. PROJECT.md §3 절대 제약 · §4 게이트 예외 E-1~E-4
3. docs/phases/phase-2-5/phase-2-5-status.md (ENTRY-1)

[1] 🔴 최우선 — Observer 도달 확인
   ListAgents 로 PAB-Observer 세션이 떴는지 본다. 이틀째 부재였다.
   떴으면 OB2-E 수령을 SendMessage 로 고지한다(파일은 이미 양측 배치, md5 7549f19f…).
   · E-1 probe ① 대행 / E-2 회전 작업창 = T-4 차단 해제 경로
   · E-4 = D-1~D-4 재요청 (D-2 선등재 금지는 편입 전 필수)
   ⚠️ Observer 가 계속 안 열리면 사용자에게 대체 전달 수단을 물을 것

[2] 승인 대기 2건 — 사용자 판단
   ⑴ 00_MOC/TYPES/SOURCE.md 삭제 (판정 확정, 정본 vault 쓰기)
      근거 ①schema 가 "SOURCE=원본 immutable 보존 전용" 으로 규정(양측 동일 문안)
           ②SOURCE 노트 40/40 이 DOMAINS·TOPICS 로 이미 도달 — TYPES MOC 없이 항해 완결
           ③_INDEX.md 는 type:[[INDEX]] 인데 TYPES/INDEX.md 는 없다 — 같은 "전용" 규정을
             받은 둘 중 INDEX 쪽이 규정대로고 SOURCE 가 잔재다
           ④[[SOURCE]] 참조가 자기 자신 1건뿐 → 삭제해도 broken 0
   ⑵ T-7 잔여 — 22건 일괄 갱신(정본 쓰기) · G2_infra E2E

[3] MOC 역할 분리 — Prove 회신 대기 중
   합의된 축: 분류 체계 = obsidian / 분류 행위 = 생성자
   · 상수는 갈리지 않았다(값 동일). 단 위치가 다르다 — 우리 moc.py:16, Prove paths.py:82
   · ⭐ ⒜안이 이미 절반 구현돼 있다: _schema.json 이 vault 안(40_Templates/)에 있고
     코드가 읽어 강제한다. ⒜ = "MOC_TYPE_NAMES 를 schema 로 합치기" 이지 신규 설계가 아니다
     ⚠️ 8종(계약) ≠ 6종(MOC 분류 축). 차이가 어디에도 안 적혀 있고 사람 머릿속에만 있다
   · 순서 제안: ⒞ 통지 의무 즉시 → ⒜ schema 통합 → ⒝ 게이트(JUDGE_DIRS 확대, 기준선 재측정)
   · 🔴 회원 vault 경계: 체계·_schema.json 은 우리, 회원 vault 안 MOC 실물은 그 vault 소관.
     단 조건 — 각 회원 vault 의 _schema.json 이 정본의 복제여야 한다

[4] T-4 — E-1·E-2 회신이 오면 즉시 착수 가능
   1단계 완료(11f44b0, _security 교체→병합). 2~4단계는 Observer 조율 + 사용자 승인
   ⚠️ pabadmin 은 config admin 이라 rev 롤백 불가 → 순서상 마지막
   ⚠️ 회전 검증은 새 세션으로 — "아직 동기화된다" 는 성공의 증거가 아니다

[5] 기록 정정 2건
   · status.md backend_dev: active → not_spawned (팀원 0명 실측)
   · current_state: IN_PROGRESS → SSOT 정규 상태 코드로

[6] 미해결·판단 대기
   · frontmatter.py:122 _try_obsidian_create() 제거 — AC-3/R-07 위반 경로. backend-dev 위임
     (정본 Untitled* 0건 = 미발생. 이유는 "앱이 꺼져 있어서" 로 추정, 우리 환경 재측정 미수행)
   · validate.py:126 unresolved 분기 제거 검토 (bf40027 가드로 급한 불은 껐다)
   · interop/README.md §5 대칭 배치 문안 정리
   · docs/history/ 추적 여부 (PUBLIC 공개가 비가역)
   · G3_smoke 장애 주입 — tester 스폰 + 사용자 승인(서비스 중단 수반)
```

---

## 6. 인계 시 놓치기 쉬운 것

### 6.1 외부 채널 상태

| 채널 | 최근 | 우리 대기 | 상대 대기 |
|---|---|---|---|
| **PAB-Observer** | **OB2-E 발신(09-08)** · ⚠️ **세션 부재로 미도달** | **E-1~E-5 전건** | — |
| **PAB-Prove** | PO11 종결(09-07) · MOC 역할 분리 문의(09-10) | **MOC 회신** | — (미회신 0건) |
| **PAB-v4** | 회원 권한 조율 종결(09-07) | — | — (착수 안 함) |

⚠️ **`OB3`는 승격 실행 통지 전용으로 양측 표에 명시 예약**돼 있다. 그 외 미래 번호는 예약하지 않는다.

### 6.2 ⭐ 이번 세션의 실패 형태 — **층을 건너뛰는 것**

| 누가 | 무엇 | 실체 |
|---|---|---|
| **Team Lead** | v4에 *"미러도 소문자라 경로 매칭 위험"* 경고 | **doc id 층에서 본 것을 미러 층에 옮겼다.** 미러 실물은 **대문자**였다. v4가 설계 제약으로 등재했다가 철회 |
| Team Lead | `_security` 축출 함정을 v4에 경고 | **과대평가.** `DB_NAME=pab-llmdata` 하나만 건드려 회원 DB는 사정권 밖 |
| Team Lead | Prove `20/20`에 *"headless라 항상 폴백"* 기제 | **결론은 맞고 기제가 틀렸다.** 그쪽은 8월에 분기를 통째로 제거했다 |
| Team Lead | 경로 추정 `/srv/pab-backup/...` → 0건 | *"백업 없음"* 으로 안 읽고 스크립트에서 실경로 확인 — **규율 2번이 작동한 경우** |

> ⭐ **P-8을 지켰는데도 틀렸다.** 감사기를 만들지 않았고 CouchDB를 직접 조회했다. **틀린 건 조회한 값을 다른 층에 옮긴 것**이라 P-8이 잡을 자리가 아니었다. v4 규율에 이렇게 적혔다 — *"P-8 준수를 P-8의 검증으로 쓸 수 없다. 필요한 것은 규칙 밖의 확인, 즉 남이 보는 것이다."*
>
> ⭐ 그리고 **걸린 경위**: 스스로 잡은 게 아니라 v4의 마운트 질문에 답하려다 미러를 처음 봤다. **묻는 행위가 답하는 쪽을 고쳤다.**
>
> ⭐ Prove 기여: **도구는 자기가 답할 수 없는 질문을 거절하지 못한다.** *"185인가 206인가"* 를 물으면 감사기는 둘 중 하나를 고르게 설계되고, **실제 답인 "둘 다 아니다"는 설계 단계에서 배제된다.**

### 6.3 채널 규약 — **개선이 새 실패 창을 열었다**

`(가)` SendMessage 병행을 도입하자 **왕복 주기가 배치 주기보다 짧아져** 양측이 동시에 `PO10`을 매겼다. 각자 규약을 정확히 지켰는데 충돌했다 — **위반이 아니라 공백**이었다.

⇒ *"같은 번호가 생기면 **발신자 자기 저장소의 커밋 시각**이 이른 쪽이 갖고 늦은 쪽이 재채번"* 을 신설. **"커밋 시각"만으로는 기준 저장소가 안 정해진다**(수신본은 커밋 안 하는 규약)는 이유로 문안을 정밀화했다.

### 6.4 ⚠️ 미확정으로 남긴 것 — 확정처럼 적지 말 것

| 명제 | 상태 |
|---|---|
| `206`은 폴백 경로의 재현 가능한 실재값 | ✅ 확정 |
| *"PO2에 적힌 206이 폴백에서 나온 것"* | ◐ **추론** — 그때 앱 상태를 모른다 |
| 정본에 `Untitled*` 0건 | ✅ 확정 |
| *"앱이 꺼져 있어서 안 생겼다"* | ◐ **추론** |
| 우리 환경에서 `obsidian create`가 절대경로를 무시하는지 | ⬜ **미측정** — Prove 환경 실측(`--vault /tmp/…/skel` → `PAB-LLMDATA-prove/Untitled.md`)은 **대조 기준으로만** 쓴다 |
| `bridge_ro` 롤로 목록 밖 계정이 접근되는지 | ⬜ **미검증** — 위험 방향이라 **검증 전까지 "열린다"로 취급**. T-4 probe와 함께 |

### 6.5 v4 조율 — 지금은 무관하나 되살아날 수 있다

v4가 **회원별 vault 지원**을 준비하다 자기 결함 6~9건으로 **착수 보류**했다. 우리에게 남는 접점 셋:

1. **`T-3` 볼륨 백업이 회원 수에 비례**해 커진다(현재 7세대 9.3MB)
2. **`pab-vault-sync-collect.sh`가 미러를 직접 읽는다**(`MIRROR_DIR`, 호스트 cron 5분). 미러가 `{미러}/{id}/`로 바뀌면 노트 대신 ID 목록을 본다. ⚠️ **실행본이 `/home/oceanui/observer/scripts/`에 있어 값 변경도 Observer 조율 대상**
3. `bridge_ro`를 회원에게 주면 **정본 읽기가 열린다** — v4가 *"주지 않겠다"* 확약

### 6.6 결정 대기 (사용자)

- **`00_MOC/TYPES/SOURCE.md` 삭제** — 판정 확정, 근거 4건. 정본 vault 쓰기
- **T-7 22건 일괄 갱신** — 정본 쓰기
- **`docs/history/` 추적 여부** — PUBLIC 공개가 비가역
- **G3_smoke 장애 주입** — 가동 중 서비스 중단 수반
