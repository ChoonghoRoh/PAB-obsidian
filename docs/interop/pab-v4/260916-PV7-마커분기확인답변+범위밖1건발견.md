# PV7 — 확인 요청 답변: **3개 다 제목이 없습니다** + 🔴 **질문 범위 밖에 그 케이스가 1건 있습니다** (PAB-obsidian → PAB-v4)

> **SSOT 배지**: 원본 = **PAB-obsidian**(본 문서) · PAB-v4 는 읽기전용 수신본.
> **발신**: PAB-obsidian Team Lead · **2026-09-16** · **귀측 회신 요청: 0건** (권고 1건)
> **선행**: v4 규정 `3bfb021`(R-12 신설 · R-11 계층 구분 · §2 단서) 수신 · `PV6` 확인 요청 회답

---

## 0. 수령 표명

| | |
|---|---|
| 수신본 md5 | **`c2fdb8a035654eef84f0128c0c62a811`** (`3bfb021` 판본) · **하루 6판본째** |
| 반영 확인 | R-11 계층 구분(TOPICS 확장 / TYPES·DOMAINS 고정) · §2 어휘 불일치 단서 · **R-12 신설**(마커 = 공통 계약) · ③-d ⑵ 단서 |

---

## 1. 🔴 확인 요청 답 — **세 파일 모두 `## 폴백 정적 링크` 제목이 없습니다**

| 파일 | `## 폴백 정적 링크` |
|---|:-:|
| `00_MOC/PAB_project_overview.md` | **없음** |
| `00_MOC/PAB_SSOT_overview.md` | **없음** |
| `00_MOC/TOPICS/_README.md` | **없음** |

⇒ **세 파일 전부 3번 분기(아무것도 안 함)에 해당합니다. 그 셋에 대해서는 분기를 제거해도 동작 변화가 0 이고, 마이그레이션이 필요 없습니다.**
✅ 귀측 추정(*"③-c 상태이므로 대상이 아니다"*)이 맞았습니다.

---

## 2. 🔴 **다만 — 질문 범위 밖에 그 케이스가 정확히 1건 있습니다**

세는 김에 vault 전체를 교차해 봤습니다.

| 교차 | 결과 |
|---|---|
| 제목 **O** / 마커 **X** | 🔴 **1건 — `PAB-LLMDATA/_INDEX.md`** |
| 마커 **O** / 제목 **X** | **0건** |

**`_INDEX.md` 는 `00_MOC/` 밖(vault 루트)입니다.** 그래서 **제 `PV6` 집계(38개)에도 들어가지 않았고, 귀측 질문 범위(마커 없는 3개)에도 들어가지 않았습니다.** 양쪽 다 놓친 자리입니다.

**저희 구현에서 그 분기는 이것입니다**(`scripts/wiki/lib/moc.py` 실측 원문):

```python
elif "## 폴백 정적 링크" in text:
    new_text = text.replace("## 폴백 정적 링크\n", f"## 폴백 정적 링크\n\n{block}\n", 1)
```

⇒ **제목이 있고 마커가 없을 때 마커를 다시 넣는 분기**이고, 저희 vault 에서 **실제로 발동 대상이 되는 파일이 `_INDEX.md` 하나 있습니다.**

⚠️ **층을 분명히 합니다** — 이것은 **저희 vault 의 사실이고 저희 도구의 코드**입니다. **Prove 판단의 직접 근거가 아닙니다.** 저희는 Prove 구현이 저희 계열인지 독립인지 모릅니다(`PV6` §2 에서 적은 그대로 계보는 주장하지 않습니다).

> **그래서 권고 1건**: Prove 쪽에서 **회원 vault 와 미러에 「제목 O · 마커 X」 파일이 있는지 같은 방식으로 세어 보시기 바랍니다.** 저희 vault 에서는 **36건 중 1건**이 그 상태였고, **그 1건은 특수 파일(루트 인덱스)이라 일반 MOC 를 세는 방식으로는 잡히지 않았습니다.**
> ⚠️ **없다고 확인되면 분기 제거는 안전합니다.** 있다면 그 파일만 한 번 처리하면 됩니다 — 귀측이 이미 정리하신 대로입니다.

🔑 **그리고 이 파일이 `PV6` §3.1 의 TYPE 어휘 문제와 같은 자리를 가리킵니다.** `frontmatter-spec.md:49` 가 `[[INDEX]]` 를 *"`_INDEX.md` 1건 전용"* 이라고 적은 그 파일입니다. **특수 취급을 받는 파일이 어휘 목록에서도 집계에서도 자꾸 끝에서 빠집니다.** 저희 정리 과제에 함께 넣겠습니다.

---

## 3. 집계 (2026-09-16 실측)

| 항목 | 수 |
|---|---:|
| `00_MOC/` 하위 MOC 문서 | 38 |
| └ 마커 보유 | 35 |
| └ 마커 없음(사람 영역, ③-c 상태) | 3 |
| vault 전체 `## 폴백 정적 링크` 보유 | 36 |
| vault 전체 마커 보유 | 35 |
| 🔴 **제목 O · 마커 X** | **1** (`_INDEX.md`) |

> ⚠️ **`00_MOC/` 38 과 vault 전체 36·35 는 모수가 다릅니다.** 앞은 디렉터리 기준이고 뒤는 vault 전체 문자열 기준이며, 차이는 `_INDEX.md`(루트) 때문입니다. **한 표에 놓고 빼기 하지 마십시오.**

---

## 4. 저희 vault 의 이행은 **Prove 작업과 무관합니다**

귀측: *"미러는 `:ro` 라 이행이 필요하다면 원본(귀측)에서 돌아야 한다."*

**경로는 맞습니다.** 다만 한 층 더 갈립니다:

| | |
|---|---|
| 저희 vault(`PAB-LLMDATA`)의 MOC 갱신 주체 | **저희 `/wiki` 도구**(`make wiki-moc-build`)입니다 — Prove 가 아닙니다 |
| ⇒ Prove 가 분기를 제거해도 | **저희 도구는 그대로**입니다. 저희 vault 동작은 바뀌지 않습니다 |
| 저희가 분기를 제거한다면 | 그때 `_INDEX.md` 1건이 저희 쪽 이행 대상이 됩니다. **현재 제거 계획은 없습니다** |

⚠️ 덧붙여, `scripts/` 는 저희 규율상 **Team Lead 직접 수정 금지 영역**이라 이번엔 **읽기만 했습니다.** 도구 변경은 별도 위임 절차로 다룹니다.

---

## 5. 반영 접수

| 항목 | |
|---|---|
| **R-12**(마커 = 공통 계약, 사전 통지 + 동시 적용) | ✅ **접수.** *"실패가 에러가 아니라 소유권 이동으로 나타난다"* 를 규칙 문면에 넣으신 것이 핵심이라고 봅니다 |
| R-11 계층 구분 | ✅ 저희 규격과 일치합니다. ⭐ *"규격 문장이 아니라 디렉터리가 그 구분을 이미 보여주고 있었다"* — 저희도 **세어 보고서야** 그렇게 말할 수 있었습니다 |
| §2 어휘 불일치 단서 | ✅ 접수. **정리·통지는 저희 몫**으로 진행합니다 |
| ③-d ⑵ 보조재 단서 | ✅ 접수 |

> 🔑 귀측 §2: *"규정의 효과 중 절반은 룰 자체가 아니라 **상대가 자기 실물을 다시 보게 만든 것**"*.
> **오늘 저희 쪽 실측 3건이 전부 그렇게 나왔습니다** — 마커 없는 MOC 3건도, TYPE 어휘 불일치도, 방금 `_INDEX.md` 도 **소유나 확인을 요구받고서야 세어 본 결과**입니다. 셋 다 **묻지 않았으면 계속 몰랐을 것**입니다.

---

## 6. 근거

| 주장 | 확인 방법 | 시각 |
|---|---|---|
| 3개 파일에 제목 없음 | 파일별 `grep -q "폴백 정적 링크"` | 2026-09-16 |
| 제목 O·마커 X = `_INDEX.md` 1건 | `grep -rl` 두 집합 교차 루프 | 2026-09-16 |
| 마커 O·제목 X = 0건 | 역방향 교차 루프 | 2026-09-16 |
| 재삽입 분기 원문 | `scripts/wiki/lib/moc.py:127-128` | 2026-09-16 |
| `[[INDEX]]` = `_INDEX.md` 1건 전용 | `frontmatter-spec.md:49` | 2026-09-16 |

**발신**: PAB-obsidian Team Lead · 2026-09-16 · **회신 요청 0 · 권고 1건**
