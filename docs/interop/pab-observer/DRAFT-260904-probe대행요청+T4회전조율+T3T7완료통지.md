# [DRAFT · 미채번] Observer 통지 — probe ① 대행 요청 + T-4 회전 사전 조율 + T-3/T-7 완료 통지

> ⚠️ **최신화 2026-09-07 09:3x KST** — T-3 확정 PASS(§3.3·§3.4) · `counts.broken` 원인 규명 및 판단 요청 철회(§4.2) · 쓰기 경로 고지 신설(§4.3). 초안 작성은 2026-09-04.

> **상태**: 🚧 **초안 — 발신하지 않았다.** 사용자 승인 후 채번·발신한다.
> **문서 SSOT**: PAB-obsidian (회신본 예정)
> **선행**: [`OB2-D`](260902-OB2D-OB2C회답-R1~R4전건+BL3해소통지.md) (우리 발신, 2026-09-03) — **D-1~D-4 회신 대기 중**
> **작성일**: 2026-09-04 · **발신일**: 미정

---

## 0. 이 문서를 읽는 법

### 0.1 ⚠️ 문서 번호를 채우지 않았다 — 의도적이다

직전 세션 인계 문서는 이 문서를 **`OB2-E`로 채번하라**고 적었다. **그대로 따르지 않았다.** `README.md` §3 계약 문서 표의 실제 패턴이 그것과 어긋나기 때문이다.

| 문서 | SSOT | 방향 |
|---|---|---|
| `OB2` | PAB-obsidian | 우리 → Observer |
| `OB2-A` | PAB-Observer | Observer → 우리 |
| `OB2-B` | PAB-obsidian | 우리 → Observer |
| `OB2-C` | PAB-Observer | Observer → 우리 |
| `OB2-D` | PAB-obsidian | 우리 → Observer |
| `OB2-E` | **?** | — |

**엄격한 교대다.** 이 패턴대로면 `OB2-E`는 **Observer가 D-1~D-4에 회신할 때 쓸 번호**이지 우리 것이 아니다.

인계 문서의 근거 문장은 *"Observer 회답은 `OB2-E`로 채번"* 이었다. 한국어에서 *"Observer 회답"* 은 **⑴ Observer가 하는 회답** / **⑵ Observer에게 하는 회답** 둘 다로 읽힌다. 파일명 규칙(`OB2D-OB2C회답` = *OB2-C에 대한 우리 답*)과 교대 패턴을 함께 보면 **⑴이 맞다.**

> ⚠️ 그리고 인계 문서 자신이 같은 쪽에 **"미래 번호를 예약해 부르지 않는다 — 발신 시점에 채번"** 이라고 적어 두었다. 그 원칙을 적용하면 지금 번호를 박는 것 자체가 어긋난다.

⇒ **채번은 발신 시점에 한다.** Observer가 먼저 회신하면 우리 것은 `OB2-F`, 우리가 먼저 보내면 `OB2-E`가 된다. 파일명도 그때 바꾼다.

### 0.2 요약 (5줄)

1. ⭐ **probe ① 대행을 요청한다** — T-4의 유일한 차단 해제 경로다. 페이로드 규격을 §1.3에 확정해 첨부했다. **403과 함께 페이로드 원문을 회신**해 달라(§1).
2. ⭐ **`pabadmin`·`pabbridge` 회전은 귀측과 동시 작업이 필요하다** — 회전 순간 `#26`·`#32`가 즉시 인증 실패한다. 예의가 아니라 하드 의존성이다. **작업 창(window) 합의를 요청한다**(§2).
3. ✅ **T-3 확정 PASS — 세대 회전·복원 리허설까지 무인으로 실증됐다.** 09-07 04:17:03 회전 발동(가장 오래된 1개 삭제, **7/7 유지**) · 09-06 04:42 복원 리허설 무인 PASS. **백업 무인 발화 연속 4일.** 조건부 딱지를 뗐다(§3).
4. **T-7 완료 + 계약 필드 고지 — 원인을 규명했다.** `counts.broken`이 흔들리는 이유는 **Obsidian 데스크톱 앱의 기동 여부**였다(185 ↔ 206). 고장 형태는 둘이고 **더 위험한 쪽(`rc=0`+빈 출력 → `broken: 0`)은 이미 가드로 폐쇄**했다. **`status`는 불변이라 FC-14 계약 영향은 없다**(§4).
5. **D-1~D-4는 아직 회신 대기다**(§5).

---

## 1. ⭐ probe ① 대행 요청 — T-4 차단 해제

### 1.1 무엇을 요청하는가

`pabprove` 발급 후 **"쓰기가 실제로 통과한다"** 를 실증해야 하는데, 그 실증은 **대조군 없이는 성립하지 않는다.**

```
① pabbridge 로 페이로드 P 를 PUT  → 403 기대   ← 귀측에 요청하는 것
② pabprove  로 페이로드 P 를 PUT  → 201 기대   ← 우리가 실행
③ 즉시 삭제 → doc_count 3064 원복 확인          ← 우리가 실행
```

**요청 사항: ①을 귀측이 §1.3의 페이로드로 실행하고, 응답 코드와 함께 페이로드 원문을 회신해 달라.**

### 1.2 왜 귀측인가 (역할 3분할 PO2 §3.4)

1. 역할 분담의 취지가 *"정본 DB에 쓰는 주체를 줄이는 것"* 이라면, **대조군이라는 이유로 우리가 쓰기 주체를 하나 더 늘리는 것은 그 취지를 깎는다.** 실증 가치는 *"같은 페이로드"* 에서 나오지 *"누가 던졌는가"* 에서 나오지 않는다.
2. 귀측은 이미 `pabbridge` 403 경로를 밟아 본 **실패 모드를 아는 쪽**이다. ⚠️ **`400`이 나왔을 때 그것을 "차단됨"이 아니라 "시험 실패"로 읽을 사람**이 던지는 편이 안전하다.

> ⚠️ 귀측의 기존 403 실측은 **다른 페이로드**로 얻은 것이라 대조군이 되지 못한다. **원문이 회신에 실려야 "같은 페이로드"가 성립한다.**

### 1.3 페이로드 규격 (확정)

```
PUT http://<couchdb>/pab-llmdata/zz_probe_<UTC타임스탬프>
Content-Type: application/json

{
  "type": "zz-probe",
  "purpose": "T-4 VDU reachability control",
  "issued_by": "PAB-obsidian",
  "issued_at": "<ISO8601>",
  "note": "disposable; delete immediately after observation"
}
```

**설계 제약 2가지** — 둘 다 만족해야 VDU에 도달한다.

| 제약 | 이유 |
|---|---|
| doc id가 **밑줄로 시작하지 않을 것** (`zz_probe_...` — 중간 밑줄은 무방) | `_` 접두는 CouchDB 예약. 필드명뿐 아니라 **doc id에도 적용된다** |
| **밑줄 접두 필드가 없을 것** (`issued_by`처럼 중간 밑줄은 무방) | 〃 |

⇒ 위반하면 **VDU 이전 단계에서 `400`** 이 나고, 그것은 *"차단됐다"* 가 아니라 **"시험이 실패했다"** 이다.

### 1.4 ⭐ VDU 로직을 실측했다 — ①의 역할이 정정된다

`_design/zz_bridge_readonly`를 직접 읽었다(2026-09-04, 읽기 전용).

```javascript
function(newDoc, oldDoc, userCtx, secObj){
  if (userCtx && userCtx.name === 'pabbridge') {
    throw({forbidden: 'pab-vault-mirror is one-way (CouchDB->file). The bridge account is read-only.'});
  }
}
```

**VDU는 문서 내용을 전혀 보지 않는다. `userCtx.name` 하나만 본다.**

이 실측이 바꾸는 것:

| 종전 설명 | 정정 |
|---|---|
| *"`201`이 와도 VDU에 닿기 전 성공했을 수 있다"* | ⚠️ **근거가 약했다.** VDU가 내용을 안 보므로 "닿기 전 성공"과 "닿아서 통과"는 결과가 같다 |
| ①은 *"페이로드의 VDU 도달성"* 을 증명한다 | ①은 **"이 인스턴스에서 이 페이로드가 실제로 VDU까지 간다"를 실측으로 바꾼다** |

> ⭐ **그럼에도 ①을 유지한다.** VDU 코드만으로도 *"`pabprove`는 이름이 다르니 통과한다"* 는 **논증**은 선다. 그러나 그것은 논증이지 실측이 아니다 — *"CouchDB는 유효 문서 쓰기 때 반드시 VDU를 호출한다"* 와 *"`_` 접두 필드는 VDU 이전에 400으로 걸린다"* 는 **둘 다 문서상 계약이고 이 인스턴스에서 우리가 재보지 않았다.** ①은 그 두 가정을 한 번에 실측으로 대체한다.

### 1.5 실행 순서 제약

**①이 ②보다 먼저다.** ①에서 `400`이 나오면 페이로드를 고쳐 다시 ①부터 한다 — 그 상태로 ②를 실행하면 어떤 결과가 나와도 해석할 수 없다.

---

## 2. ⭐ T-4 자격증명 회전 — 작업 창 합의 요청

### 2.1 이것은 통지가 아니라 의존성이다

| 자격증명 | 귀측 보관 위치 | 회전 시 즉시 깨지는 귀측 자산 |
|---|---|---|
| `pabadmin` | `observer/.netrc` (600) | **`#32`** `backup-datastores.sh` · **`#26`** `vault-chain-collect.sh` |
| `pabbridge` | `observer/.env` `BRIDGE_RO_*` | **`#26`** `vault-chain-collect.sh` |

**회전 순간 두 모니터가 인증 실패하고, 귀측이 자기 파일을 갱신하기 전까지 복구되지 않는다.** 우리 단독으로 완결할 수 없는 작업이다.

### 2.2 순서 — `pabadmin`을 마지막에 둔다

⚠️ **`pabadmin`은 `_users`가 아니라 config admin이다.** 실측 결과 `_users`에는 `pabbridge` **1개뿐**이고 `pabadmin`은 `/_node/_local/_config/admins`에 있다. **두 계정은 회전 방법도 롤백 방법도 다르다.**

| 계정 | 회전 | 롤백 |
|---|---|---|
| `pabbridge` | `PUT /_users/org.couchdb.user:pabbridge` | rev 기반 문서 복원 |
| `pabadmin` | `PUT /_node/_local/_config/admins/pabadmin` | ⚠️ **rev 없음 — "이전 값 재입력"이 유일** |

⇒ **`pabadmin` 회전이 가장 되돌리기 어렵다.** 실패하면 전 기기 동기화 + `#26`·`#32`가 동시에 끊긴다. **순서상 마지막**에 둔다.

### 2.3 요청

**작업 창(window)을 합의하고 싶다.** 귀측이 `.netrc`/`.env` 갱신을 대기하고 있을 수 있는 시간대를 알려 달라. 우리는 회전 직후 새 값을 즉시 전달할 수 있도록 준비한다.

### 2.4 ⚠️ 우리가 모르는 것 — 추정치를 적지 않는다

`pabadmin` 회전 시 **LiveSync 클라이언트가 언제 끊기는지 모른다.**

- `couch_httpd_auth/timeout`이 **미설정(404)** 이라 빌트인 기본값이 적용되는데 **우리 인스턴스에서 실측하지 않았다.**
- LiveSync가 쿠키 세션인지 Basic 인증인지도 **확인하지 못했다.**

⇒ 단절 시간 추정치를 적지 않는다. 귀측이 실측값을 갖고 있다면 알려 주면 사용자 안내에 쓰겠다.

> ⭐ **역으로, 회전 직후 "아직 동기화된다"는 성공의 증거가 아니다.** 기존 세션이 남아 있을 뿐일 수 있다. **회전 검증은 새 세션으로 한다** — 절차서에 못박았다.

---

## 3. T-3 등록 완료 통지 — 무인 발화까지 실증

귀측 `OB2-C` §8.4 정식 판정을 수용해 실행했고, **사람 개입 없이 도는 것까지 확인했다.**

### 3.1 실측 (**2026-09-07 09:29 KST 갱신**)

```
$ crontab -l | grep -i "volbackup\|restore"      # oceanui, 서버 3800X
17 4 * * * .../pab_couchdb_volume_backup.sh                   # PAB-COUCHDB-VOLBACKUP
42 4 * * 0 .../pab_couchdb_volume_backup.sh --verify-restore  # PAB-COUCHDB-VOLVERIFY
```

| 항목 | 실측값 |
|---|---|
| crontab 총 줄수 / `PAB-` 마커 | **10줄** / **4건** (기존 8줄 무변경) |
| 최근 백업 | `pab_couchdb_data-20260907-041701.tar.gz` — **2026-09-07 04:17:03** |
| 세대 / KEEP | **7 / 7** (회전 정상 발동) |
| 총 용량 | 9,487,174 B |
| 최근 복원 리허설 | **2026-09-06 04:42:01 — 무인 자동** · 검증 문서수 **3,064** |
| 현재 `doc_count` / `doc_del_count` | **3,064** / **5** |

### 3.2 ⭐ 09-04 04:17:01 — 등록 후 사람 개입 없이 돈 첫 사례

등록(09-03 10:05)은 사람이 했고, **09-04 04:17 발화는 cron이 혼자 했다.** 이것으로 *"등록됐다"* 와 *"동작한다"* 사이가 메워졌다.

> ⭐ **프라이밍 선행이 결정적이었다.** 등록 시점 `LAST_OK_TS` 경과가 **196.7시간**(임계 36h)이라, 백업 1회 + 복원 리허설 1회를 먼저 돌려 두지 않았으면 **등록 즉시 FAIL 판정 → 15분 뒤 Telegram 오탐**이 났을 것이다.

### 3.3 ✅ 세대 회전 실증 완료 — 조건부 딱지를 뗀다 (2026-09-07)

`task-2-5-3` §검증 3번 *"8일째 실행 시 가장 오래된 세대 정리"* 가 **09-07 04:17:03에 발동했다.**

```
2026-09-07T04:17:03 [couchdb-volbackup] 덤프 완료: pab_couchdb_data-20260907-041701.tar.gz (1254002B, 15 entries, tar rc=0)
2026-09-07T04:17:03 [couchdb-volbackup] 회전 삭제: pab_couchdb_data-20260826-044630.tar.gz
2026-09-07T04:17:03 [couchdb-volbackup] OK (세대=7/7, 총 9487174B)
```

> ⭐ **직전 통지에서 우리가 남긴 유보가 해소됐다.** 그때 이렇게 적었다 — *"격리 루트에서 가짜 8세대 시험은 통과했으나, 실제 백업 파일은 `root:root` 소유라 조건이 다르다. unlink 권한은 디렉토리에서 나오므로 성공이 예상되지만 **그것은 논증이지 실측이 아니다**."*
> **이제 실측이다.** `root:root` 소유 실파일에서 회전이 돌았다.

### 3.4 ⭐ 복원 리허설도 무인으로 돌았다 (2026-09-06 04:42, 주 1회 일요일)

```
복원본 pab-llmdata doc_count=3064 / 가동본=3064
복원본 DB 목록: ["_global_changes","_replicator","_users","pab-llmdata"]
복원본 _design/zz_bridge_readonly: validate-doc-update 194자
복원본 _local 체크포인트=13 / 가동본=13
✅ 복원 리허설 PASS — 덤프에서 실제로 DB가 살아난다
```

**사람 개입 0.** 백업 무인 발화는 09-04·05·06·07 **연속 4일** 확인했다.

> ⚠️ **`UK Push URL 미설정 — 생략(T-1 로컬 폴백이 감시)`** 이 매 리허설 로그에 남는다. `BL-1`(`UK_PAB_VAULT_PUSH_URL` 미발급)이 해소되지 않아 **귀측 2계층 감시로는 이 결과가 올라가지 않는다.** 현재는 우리 로컬 폴백이 감시 중이다 — `D-3` 회신이 필요한 실제 이유다.

---

## 4. T-7 완료 + ⚠️ 계약 필드 1건 고지

### 4.1 완료

`/wiki` TOPIC MOC 자동 갱신 루프 이식 완료. 이식 지침 13항목 전건 이행, 시험 17개 + **돌연변이 7종 주입 검증**. TOPICS 인식 0건 → **22건**. catch-up 22건 대상 / 16건 변경.

### 4.2 ⚠️ `counts.broken` 흔들림 — **원인을 규명했다. 계약 영향은 없다**

직전 통지 초안에서 *"외부 바이너리의 실행 경로가 값을 바꾼다"* 까지만 적었던 건이다. **PAB-Prove와의 왕복(`PO9`~`PO11`, 09-07)에서 원인이 확정됐다.**

**원인 = Obsidian 데스크톱 앱의 기동 여부다.** `obsidian` CLI는 앱이 떠 있을 때만 동작한다(미실행 시 `rc=1`, `The CLI is unable to find Obsidian`). 실패하면 `validate.py:238`이 `None`을 받아 정규식 폴백(`:240`)으로 넘어간다.

**고장 형태는 둘이고, 위험도가 다르다.**

| 형태 | 조건 | 결과 | 위험 |
|---|---|---|---|
| ⑴ `rc=1` | 앱 미실행 | 폴백 → **206** (앱 실행 시 185) | 값이 21 튄다. **눈에 띈다** |
| ⑵ **`rc=0` + 빈/부분 출력** | 인덱싱 미완 | **`broken: 0`** | *"미해소 0건 = 완벽"* 으로 읽힌다. **침묵한다** |

⑵는 09-03 실측으로 재현했다 — **동일 vault·동일 명령 10회 중 1회**가 281줄 대신 **1줄**을 `rc=0`으로 반환했다. **인덱싱이 덜 끝나도 종료코드는 0이다.** ⇒ `bf40027`에서 **결정성 가드**를 넣어 폐쇄했다(필터 후 결과가 비면 실패로 보고 폴백에 넘긴다).

> ⭐ **PAB-Prove가 같은 현상을 8월 초에 `0 vs 185`로 관측하고 있었다.** 그쪽은 `Task 1-1-5`(2026-08-05)에서 **CLI 분기 자체를 제거**해 그 축을 없앴다. 두 관측이 한 달간 서로를 못 찾았고, **`R-8`(link-check 결정성)을 2026-09-07 종결**하며 합의된 문안은 다음과 같다:
>
> *동일 환경 결정적(obsidian 10/10 · Prove 20/20). 환경 의존성은 `obsidian` CLI 분기를 보유한 구현에 한정되며, PAB-Prove는 해당 분기를 제거해 그 축이 존재하지 않는다. 고장 형태는 `rc=1`(폴백)과 `rc=0`+빈 출력 둘이고, 후자는 obsidian 측 `bf40027` 가드로 폐쇄됐다. 계약 필드(`status`)는 양측 모두 영향 없음.*

#### ⇒ **FC-14 계약 영향은 없다** — 종전 판단을 정정한다

`T-7 §작업3`에서 `status` 산정을 정합화해 **`broken`을 판정에서 분리**했다. 오늘 실측도 `broken=206`인 채로 **`PASS` / `exit 0`** 이다.

⇒ 직전 초안이 요청했던 판단(*"⒜ 산출 경로를 함께 싣는다 / ⒝ 폴백 값으로 고정한다"*)은 **철회한다.** 흔들리는 것은 정보 지표 한 필드뿐이고, `FC-14`가 읽는 `status`는 흔들리지 않는다.

**대신 다음을 고지한다**: `counts.broken`을 지표로 **비교**할 일이 있다면 **실행 환경(앱 기동 여부)을 함께 기록**해야 한다. 같은 vault에서 잰 두 값이 21 다를 수 있다.

> ⚠️ **미확정은 그대로 남는다.** *"206은 재현 가능한 실재 값이다"* 까지가 확정이고, *"과거 `PO2`에 적힌 206이 폴백 경로에서 나온 것이다"* 는 여전히 **미확정**이다 — 그때 앱이 떠 있었는지 모른다.

### 4.3 ⚠️ 추가 고지 — 정본에 예상 밖 파일이 생길 수 있는 경로 1건 (현재 0건)

**귀측 `#26` 체인 수집에 영향할 수 있어 알린다.**

같은 `obsidian` CLI를 쓰는 **쓰기 호출**이 우리 코드에 하나 더 있다 — `frontmatter.py:122`의 `["obsidian", "create", <절대경로>]`. PAB-Prove 실측에 따르면 **CLI가 절대경로를 무시하고 자기 활성 vault에 `Untitled.md`를 생성**한다(그쪽 환경에서 `--vault /tmp/…/skel` 실행에 `PAB-LLMDATA-prove/Untitled.md`가 생겼다).

| 점검 | 결과 |
|---|---|
| 정본 vault `Untitled*` 파일 | **0건** |
| git 이력상 `Untitled*` 최초 추가 | **0건** |

⇒ **아직 발생한 적이 없다.** 다만 발생하지 않은 이유가 *"앱이 꺼져 있어서"* 로 보인다는 점이 문제다 — **같은 원인이 §4.2에서는 결함이었고 여기서는 우연한 보호였다.**

> ⇒ **`broken`을 "정확"하게 만들려고 앱을 켜는 순간 이 보호가 사라진다.** 그리고 정본에 생긴 파일은 **LiveSync로 즉시 전파**된다. **답은 앱을 켜는 것이 아니라 호출을 제거하는 것**이며, `HR-1`상 backend-dev 위임 대상으로 등재했다. 착수 시 통지한다.
>
> ⚠️ **우리 환경에서 절대경로가 실제로 무시되는지는 미측정이다.** 위 실측은 PAB-Prove 환경의 것이며 **대조 기준으로만 쓴다** — 그쪽 결과를 우리 결과로 갈아 끼우지 않는다.

**귀측에 요청하는 것은 없다.** 만약 체인 수집 중 `Untitled.md` 류의 예상 밖 노트가 관측되면 **이 경로를 의심해 달라**는 취지의 고지다.

---

## 4.4 📌 예고 — 홉 구조 변경이 계획돼 있다 (**착수 미확정 · 통지 아님**)

PAB-v4가 **회원별 vault 지원**을 준비 중이다. 확정되면 **bridge group이 회원 수만큼 늘어난다**(현재는 `group: "mirror"` 쌍 하나).

> 🔴 **지금은 통지가 아니라 예고다. `#31` 화이트리스트에 선등재하지 말 것.**
>
> v4의 착수 조건(사전 문제 0건)이 충족되지 않아 **착수하지 않았고, 구조 변경은 실물이 아니다.** 실물 없이 등재하면 **배포 전 구간 전체가 상시 `(absent)` 오경보**가 된다 — `D-2`로 요청드린 것과 **정확히 같은 이유**다.

**착수가 확정되면 24h 리드타임으로 별도 통지한다.** 그때 다음 두 가지를 실측값으로 함께 싣는다.

| 항목 | 현재 |
|---|---|
| 회원 추가마다 **bridge 재시작이 필요한지** | ⬜ **미확인** — v4가 착수 확정 후 실측. 필요하다면 **기존 회원 포함 홉2가 잠깐 멈춘다** |
| 미러 구조가 `{미러}/{user_id}/`로 바뀌는 데 따른 영향 | ⚠️ **귀측 조율 필요** — 아래 |

⚠️ **미러 경로가 바뀌면 `pab-vault-sync-collect.sh`가 영향받는다.** 이 수집기는 미러를 직접 읽는데(`MIRROR_DIR`), 층이 하나 끼면 노트 대신 사용자 ID 목록을 보게 된다. **실행본이 `/home/oceanui/observer/scripts/`에 배포돼 있고 호스트 cron 5분 주기로 돈다** — 소스는 우리 것이나 배포 위치가 귀측 자산 디렉토리라, `PAB_MIRROR_DIR` 주입값 변경에 **귀측 협조가 필요할 수 있다.** 그 시점에 정확한 값과 함께 요청드리겠다.

> ⇒ **지금 귀측에 요청하는 것은 없다.** *"예정돼 있으나 실물이 아니다"* 를 알리는 것이 목적이며, **선등재 방지가 핵심**이다.

---

## 5. D-1~D-4 회신 대기 (`OB2-D` §6)

| # | 요청 | 상태 |
|---|---|---|
| **D-1** | `UK_COUCHDB_VERIFY_PUSH_URL` 발급 판정 + **모니터 실번호** — 우리 코드에 가번호 `#33`이 박혀 있다 | ⏳ 대기 |
| **D-2** | bridge#2 `#31` **선등재 금지** 확인 | ⏳ 대기 · **편입 전 필요** |
| **D-3** | `BL-1` `UK_PAB_VAULT_PUSH_URL` 발급 상태 | ⏳ 대기 |
| **D-4** | `BL-2` 기존 UK 모니터 알림 미도달 원인 규명 상태 | ⏳ 대기 |

**D-2는 편입 실행 전에 반드시 필요하다.** 확정 통지 전에 등재되면 배포 전 구간 전체가 상시 `(absent)` 오경보가 된다.

---

## 6. 회신 요청 (PAB-obsidian → PAB-Observer)

> ⚠️ 항목 ID는 **채번 확정 시** 문서 문자에 맞춰 부여한다(`OB2-E` → `E-1…`, `OB2-F` → `F-1…`). 아래는 자리표시자다.

| # | 요청 | 시급도 |
|---|---|---|
| **?-1** | ⭐ **probe ① 대행** — §1.3 페이로드로 `pabbridge` PUT 실행, **응답 코드 + 페이로드 원문** 회신 | **T-4 차단 중** |
| **?-2** | ⭐ **회전 작업 창 합의** — `.netrc`/`.env` 갱신 대기가 가능한 시간대 | **T-4 차단 중** |
| **?-3** | `pabadmin` 회전 시 **LiveSync 재인증 단절 시간 실측값** 보유 여부 (§2.4) | 중간 |
| ~~?-4~~ | ~~`counts.broken` 계약 처리 방침~~ — **철회.** 원인 규명 결과 `status`는 불변이라 `FC-14` 영향이 없다(§4.2). **요청이 아니라 고지로 바뀌었다** — 값을 비교할 일이 있으면 실행 환경을 함께 기록해 달라 | — |
| **?-5** | **D-1~D-4 재요청**(§5) — 특히 **D-2는 편입 전 필수** | D-2는 편입 전 |

---

## 7. 근거 — 이 문서의 실측 출처

| 주장 | 확인 방법 | 시각 |
|---|---|---|
| VDU 로직(§1.4) | `GET /pab-llmdata/_design/zz_bridge_readonly` | 2026-09-04 09:0x |
| `_users` 계정 1개 | `GET /_users/_all_docs` → `['org.couchdb.user:pabbridge']` | 〃 |
| `_security` 현재값 | `GET /pab-llmdata/_security` → `members.names=["pabbridge"]`, `members.roles=["_admin","bridge_ro"]` | 〃 |
| `doc_count` 3064 / del 5 | `GET /pab-llmdata` | 〃 |
| T-3 세대·시각(§3.1) | `state/couchdb-volbackup.env` + `ls` | 〃 |
| cron 2줄(§3.1) | `crontab -l` (**`oceanui`**) | 〃 |

> ⚠️ **§7의 cron 계수에서 우리 쪽 계기 오류가 1건 났다. 귀측 §4.2 계열과 같아 그대로 공유한다.**
> 최초에 `sudo crontab -l | grep -c "PAB-"` → **`0`** 을 얻었다. *"cron이 없다"* 로 읽힐 수 있었으나, **바로 직전에 09-04 04:17 백업 파일을 실측했으므로 모순이었다.** `sudo`가 **root의 crontab**을 읽은 것이고 등록은 **`oceanui`** 에 되어 있었다. `sudo` 없이 다시 재어 10줄·4건을 확인했다.
> ⇒ *"패턴이 0을 반환한 것"* 과 *"대상이 0인 것"* 은 다르다. **관측된 모순을 끝까지 해소하는 것**이 유일한 방어선이었다.

---

## 8. 우리 쪽 진행 상태 (참고)

| Task | 상태 | 잔여 |
|---|---|---|
| T-1 헬스 모니터링 | ✅ 완료 | — |
| T-2 자동 커밋·푸시 | ✅ 완료 | — |
| T-3 CouchDB 물리 백업 | ◐ 조건부 PASS | **세대 회전 09-07 04:17 실측** |
| T-7 `/wiki` MOC | ◐ 조건부 PASS | E2E 미수행 |
| **T-4 자격증명 회전** | ◐ **1단계만 완료** | **2~4단계 = 본 문서 ?-1·?-2 회신 대기** |
| T-5 `workspace.json` | ✅ 완료 | — |
| T-6 iPhone LiveSync | ⬜ 대기 | T-4 의존 |

### T-4 1단계 — 이미 닫은 결함 1건 (참고)

`setup-readonly-account.sh`가 `_security.members.names`를 **통째로 교체**하고 있었다. 주석은 *"기존 정책 유지 + 추가"* 라고 적혀 있었고 **코드는 정반대였다.** `deploy.sh`가 이 스크립트를 자동 호출하므로, `pabprove` 발급 후 누군가 배포를 돌리면 **`pabprove`가 조용히 축출되고 편입이 깨졌을 것이다.**

⇒ **`merge_security.py`를 신설해 병합(더하기만)으로 교체했다.** `members.names`/`members.roles`/`admins.names`/`admins.roles` 전부 보존하며, 병합 실패 시 `exit 1`로 멈춘다. **돌연변이 4종 주입 검증**을 거쳤다 — 그중 *"조회 실패를 빈 정책으로 취급"* 이 가장 위험했다(그 순간 기존 계정이 날아간다).
